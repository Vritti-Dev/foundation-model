# Normalize line endings: store text files with LF in the repo, regardless of
# the contributor's OS. This silences the CRLF warnings Git emits on Windows
# checkouts. Binary files are auto-detected and left untouched.
* text=auto eol=lf
